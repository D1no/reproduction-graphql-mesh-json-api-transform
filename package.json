{
  "name": "reproduction",
  "scripts": {
    "start": "npm run serve_mock_api & npx mesh dev",
    "serve_mock_api": "npx json-server --quiet __mock_api_route.json --port 4100"
  },
  "devDependencies": {
    "json-server": "^0.17.0"
  },
  "dependencies": {
    "@graphql-mesh/cli": "^0.48.0",
    "@graphql-mesh/json-schema": "^0.23.4",
    "graphql": "^15.8.0"
  }
}
